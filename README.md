# Kargo Takip OOP

Basit bir JavaScript OOP projesi — kargo takip simülasyonu.

## Proje Hakkında

Bu proje, nesne yönelimli programlama (OOP) prensiplerini kullanarak bir kargo takip sistemi modellemektedir. Yeni başlayanlar için
anlaşılabilir, iyi yapılandırılmış bir örnek sunar: sınıflar, modüler dosya düzeni ve servis katmanlarıyla temel OOP kavramlarını
öğrenmeye uygundur. Proje küçük, genişletilebilir ve gerçek dünya senaryolarına (yeni şirketler ekleme, bildirimler, veritabanı
entegrasyonu) kolayca adapte edilebilir.

Hedef: Öğrenmeye yöneliktir; eğitim amaçlı kullanım ve geliştirilmeye açıktır.

## İçerik

`app/` : Uygulama giriş dosyaları
`companies/` : Kargo şirketlerine ait modüller
`core/` : Temel sınıflar ve mantık
`services/` : Yardımcı servisler
`utils/` : Genel yardımcı fonksiyonlar
`index.html` : Proje ana sayfası

## Çalıştırma

1. Proje klasörünü açın:

```
cd "c:\\Users\\hamdi\\Desktop\\react_tutorial\\js\\js öğreniyorum\\Kargo Takip OOP"
```

2. Tarayıcıda `index.html` dosyasını açın veya basit bir sunucu çalıştırın:

```
python -m http.server 8000
# sonra tarayıcıda http://localhost:8000/ adresini açın
```

Alternatif: VS Code kullanıyorsanız `Live Server` eklentisi ile `index.html` dosyasını canlı görüntüleyebilirsiniz.

## Katkıda Bulunma

- Değişiklik yapıp pull request gönderin.

## Uzaktan depo

Bu proje GitHub'da şu adreste yer alıyor:
https://github.com/hamdidrsy/Kargo-Takip-OOP

---

Oluşturan: hamdidrsy

# Kargo Takip OOP

Basit bir JavaScript OOP projesi — kargo takip simülasyonu.

## İçerik

- `app/` : Uygulama giriş dosyaları
- `companies/` : Kargo şirketlerine ait modüller
- `core/` : Temel sınıflar ve mantık
- `services/` : Yardımcı servisler
- `utils/` : Genel yardımcı fonksiyonlar
- `index.html` : Proje ana sayfası

## Çalıştırma

1. Proje klasörünü açın:

```
cd "c:\\Users\\hamdi\\Desktop\\react_tutorial\\js\\js öğreniyorum\\Kargo Takip OOP"
```

2. Tarayıcıda `index.html` dosyasını açın veya basit bir sunucu çalıştırın:

```
python -m http.server 8000
# sonra tarayıcıda http://localhost:8000/ adresini açın
```

Alternatif: VS Code kullanıyorsanız `Live Server` eklentisi ile `index.html` dosyasını canlı görüntüleyebilirsiniz.

## Katkıda Bulunma

- Değişiklik yapıp pull request gönderin.

## Uzaktan depo

Bu proje GitHub'da şu adreste yer alıyor:
https://github.com/hamdidrsy/Kargo-Takip-OOP

---

Oluşturan: hamdidrsy
